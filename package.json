{
  "name": "power-monitoring-and-control-system",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"frontend,backend\" --prefix-colors \"cyan,green\" --prefix \"[{name}]\" \"npm run dev --workspace apps/frontend\" \"npm run dev --workspace apps/backend\"",
    "dev:frontend": "npm run dev --workspace apps/frontend",
    "dev:backend": "npm run dev --workspace apps/backend",
    "build": "npm run build --workspace apps/frontend && npm run build --workspace apps/backend",
    "lint": "npm run lint --workspace apps/frontend && npm run lint --workspace apps/backend"
  },
  "devDependencies": {
    "@esbuild/darwin-arm64": "^0.27.3",
    "concurrently": "^9.2.1"
  }
}
